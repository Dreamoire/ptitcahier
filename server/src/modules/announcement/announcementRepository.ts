export type AnnouncmentCategory = {
  id: number;
  name: string;
};

export const findAnnouncementCategories = async (): Promise<
  AnnouncmentCategory[]
> => {
  // Placeholder implementation, replace in step 3
  return [];
};
